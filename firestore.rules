rules_version = '2';
// The client never talks to Firestore directly: all access is mediated by the
// API server using its service account. Deny everything else.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} { allow read, write: if false; }
  }
}
